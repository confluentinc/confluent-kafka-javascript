// H1 placeholder. H2 fills in:
//   - parseConfigString(s: string): AwsOAuthBearerConfig
//     parses the verbatim librdkafka `sasl.oauthbearer.config` string
//     (space-separated `key=value`) into a typed AwsOAuthBearerConfig.
export {};
