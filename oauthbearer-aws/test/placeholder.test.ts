// H1 placeholder. H2 deletes this file when real tests arrive
// (jwt.test.ts and config-string-parser.test.ts).
//
// Without at least one test file, `npm test` exits non-zero with
// "no tests found", which would break the H1 exit gate.

describe('oauthbearer-aws scaffolding', () => {
  it.todo('H2: implement subFromJwt + parseConfigString and their tests');
});
